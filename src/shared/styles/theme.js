import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#5048E5",
    secondary: "#6B7280",
    error: "#D14343",
  },
};

export default theme;
